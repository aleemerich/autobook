# Book Data Template

This directory contains versioned starter files for a new local book workspace.

`book_data/` itself is runtime workspace data and is ignored by Git except for
its `.gitkeep` placeholder. When a new work is created, copy the files from
this template into `book_data/` or let the Autobook wizard initialize them.
